# 窗口布局管理器 v1.1.0 用户手册与技术说明

> 1.1.0 新增“本地用户”管理：先选择用户，再选择该用户的布局。此功能仅用于本机整理，不涉及联网、登录或账号同步。

文档版本：1.1  
适用软件版本：v1.1.0  
适用系统：Windows 10 / Windows 11  
最后同步：2026-09-07

## 1. 软件简介

窗口布局管理器是一款 Windows 桌面窗口编排工具。它可以按照预先保存的顺序启动浏览器窗口或普通桌面应用，并把每个窗口移动到指定显示器上的指定位置和尺寸。

它适合以下场景：

- 每天需要打开固定的一组网站、办公软件或行情工具；
- 使用多显示器，希望窗口每次都出现在固定屏幕；
- 需要在“办公”“交易”“演示”等多套窗口组合之间切换；
- 同一个 Chrome 或 Edge 需要同时打开多个独立网址窗口；
- 已经手动摆好一个窗口，希望直接读取其程序、屏幕、位置和尺寸。

软件的核心单位是“布局”。一个布局包含若干“窗口项目”，每个窗口项目记录应用程序、网址或参数、目标显示器、窗口位置、启动等待时间和查找窗口超时时间。

### 1.1 v1.1.0 更新摘要

- 新增本地用户管理和用户分组，可为不同用户分别管理布局；
- 用户与布局数据仅保存在本机，不涉及联网登录或账号同步；
- 新增窗口刷新：右键任一窗口选择“刷新窗口”即可预览并同步位置、大小；浏览器窗口还会同步网址，读取不完整时只补充、不删除；3秒后捕获浏览器窗口会尝试读取全部标签页；
- 新增顶部工具栏最右侧“导出日志”，导出环境、屏幕、布局、窗口追踪和完整日志用于排查问题；
- 新增“关闭当前布局”，移除独立的“关闭前一布局”菜单项；
- 优化多选删除和关闭确认清单，确认列表支持滚轮滚动并去除不自然的滚动条边线；
- 修复底部日志框无法使用鼠标滚轮上下滚动的问题；
- 延续 1.0.4 的多布局、浏览器多网址、窗口捕获、更新流程和界面布局优化。

## 2. 重要概念

第一次使用前，建议先理解以下三个概念：

| 概念 | 含义 |
| --- | --- |
| 当前显示布局 | 当前在“布局方案”下拉框中选择、正在主列表里查看或编辑的配置。选择布局本身不会自动启动或关闭任何窗口。 |
| 本地用户 | 仅用于在本机给布局分组的名称，不是登录账号。选择用户后，“布局方案”只显示该用户拥有的布局。 |
| 当前启用布局 | 最近一次通过“全部启动”完整启用的布局，或通过“启动”部分打开后由软件记录的窗口集合。 |
| 窗口追踪 | 软件记录自己实际启动成功的窗口身份，用于布局切换时安全地关闭旧布局。它不是布局配置，重启后会重新验证。 |

因此，**切换下拉框只改变正在查看的配置，不等于切换桌面上的窗口**。需要真正启用新布局时，请点击“全部启动”。

## 3. 系统要求与文件说明

### 3.1 系统要求

- Windows 10 或 Windows 11；
- Microsoft .NET Framework 4.8；
- 普通 Windows 桌面会话；
- 如需控制以管理员身份运行的目标程序，本软件通常也需要以管理员身份运行。

### 3.2 安装与日常运行

发布版会提供 `WindowLayoutManager-Setup-v1.1.0.exe` 安装包。首次使用时双击该安装器，按向导完成安装；默认安装到当前用户的程序目录，不需要管理员权限。安装完成后可从开始菜单启动，也可以在安装向导中选择创建桌面快捷方式。

GitHub 的发布页面还会单独提供 `WindowLayoutManager-UserGuide.md`，可按需下载这份用户手册；软件“关于”窗口中的“打开用户手册”会打开安装目录内的同一份文件。

安装器会一并安装以下运行文件：

```text
窗口布局管理器.exe
窗口布局管理器.exe.config
用户手册.md
```

- `窗口布局管理器.exe`：程序本体，双击它启动软件；
- `窗口布局管理器.exe.config`：.NET 运行配置，不需要手动打开或修改；
- `用户手册.md`：软件“关于”窗口打开的本机使用手册。

安装器会在 Windows 的“已安装的应用”中登记卸载项。`src`、`tests`、`assets`、`tools` 和各类 `build*.ps1` 脚本仅用于继续开发、测试和重新构建，不影响日常使用。

## 4. 启动软件

1. 双击 `窗口布局管理器.exe`。
2. 软件打开后不会自动选择或启动任何布局。
3. 先从“本地用户”下拉框中选择用户。
4. 再从“布局方案”下拉框中选择该用户的布局；如果是第一次使用，可以在“用户管理”中新建用户或点击“新建”创建布局。
5. 选择布局后，主列表会显示该布局中的全部窗口项目。

同一 Windows 用户会话只允许运行一个窗口布局管理器实例。如果软件已经缩到托盘，再次双击 EXE 不会启动第二个实例，而会显示原来的主界面。

## 5. 主界面说明

### 5.1 顶部工具栏

| 功能 | 作用 |
| --- | --- |
| 添加窗口 | 在当前布局末尾添加一个新窗口项目。 |
| 3秒后捕获窗口 | 隐藏管理器 3 秒，读取倒计时结束时位于最前方的窗口；浏览器会尝试读取全部可读取标签页网址，并识别隐私或来宾模式。 |
| 一键捕获所有窗口 | 立即扫描当前桌面上的可用窗口，在确认窗口中全选、全不选或部分勾选，然后保存为新布局或替换当前布局。浏览器会尽量读取当前活动标签页网址，并识别隐私或来宾模式。 |
| 一键刷新网址 | 检查当前布局中各浏览器窗口的实际标签页网址。完整读取时会预览并同步增删；读取不完整时只补充、不删除原配置。 |
| 保存全部布局 | 把所有布局保存到本地配置文件。 |
| 打开配置目录 | 打开布局配置及运行状态文件所在目录。 |
| 关于 | 弹出关于窗口，显示当前版本和项目地址；可手动检查更新，也可以设置是否在启动时自动检查。发现新版后，更新提示窗口会完整显示更新说明，并可选择下载更新或取消。 |
| 外观 | 切换 Day（亮色）、Night（暗色）或 Auto（跟随 Windows 应用明暗设置）模式；选择会立即生效并自动保存。 |

### 5.2 布局区域

| 功能 | 作用 |
| --- | --- |
| 本地用户 | 选择当前要查看的本机用户分组。仅切换显示，不自动启动或关闭窗口。 |
| 用户管理 | 新建、复制、重命名、删除本地用户；首列复选框支持批量操作，Shift 可连续多选、Alt 可不连续多选；可按用户名称、布局数量、窗口项目数、创建时间或最后修改时间点击表头排序；拖动表头分隔线可调整列宽，向右拖动会保持左边界不动并向右扩展；右键菜单还可导出所选用户的全部布局。 |
| 布局方案 | 在当前用户下选择要查看和编辑的布局。仅切换显示，不自动启动或关闭窗口。 |
| 新建 | 创建一个空布局。 |
| 复制 | 复制当前布局及其中全部窗口，适合在相似方案基础上修改。 |
| 重命名 | 修改当前布局名称。布局名称不能和已有名称重复。 |
| 删除 | 删除当前布局及其中的窗口配置。至少必须保留一个布局。 |
| 布局管理 | 集中查看、复制、重命名、编辑、删除、导入和导出布局。 |
| 搜索窗口 | 输入名称、类型、程序、任一网址、浏览模式、其他参数或位置中的关键词，即时筛选主列表。 |

### 5.3 运行按钮

| 按钮 | 作用与范围 |
| --- | --- |
| 启动 ▼ | 启动选中项，或按浏览器窗口、Edge、Chrome、桌面应用分类启动当前布局。 |
| 关闭 ▼ | 关闭选中项、关闭当前布局，或按浏览器窗口、Edge、Chrome、桌面应用分类关闭；关闭前显示确认清单。 |
| 全部启动 | 按列表顺序启动当前布局的全部窗口；如果此前启用了另一布局，会先确认并关闭前一布局的已追踪窗口。 |
| 全部关闭 | 检查所有用户、所有布局中的全部配置项，并在确认后关闭对应的浏览器窗口或桌面应用。 |

### 5.4 主列表与日志

主列表包含“状态”“名称”“类型”“程序”“网址”和“位置”等列。多网址项目会在首个网址后显示额外数量。

- 点击任一列表头可以排序；再次点击切换升序或降序；
- 使用上方“搜索窗口”快速筛选列表，筛选只影响显示，不会修改或删除布局配置；
- 单击一行进行普通选择；
- 按住 `Shift` 点击另一行，可以连续选中两行之间的全部项目；
- 按住 `Alt` 点击，可以添加或取消不连续的多行选择；
- 点击软件内部列表以外的非按钮区域（包括列表下方或右侧空白区域），可以取消全部高亮选择；
- 双击任一行可以直接启动该窗口；
- 右键一行可以启动、关闭、刷新窗口、重命名、编辑、复制、删除、上移或下移；刷新窗口会先确认位置、大小变更，浏览器项目还会同步具体新增或删除的网址；
- 可以拖动一行改变顺序；多选后拖动其中一行，可以整体移动选中的项目；
- 底部状态栏显示当前操作结果，日志框显示逐项启动、查找、移动或关闭信息。
- 点击顶部工具栏最右侧的“导出日志”，可以导出包含软件版本、系统环境、屏幕信息、布局配置、窗口追踪记录和完整日志的诊断文件；其中可能包含程序路径、布局名称、网址和启动参数，请确认隐私后再发送。

点击列表头排序会同时更新窗口执行顺序。使用列表头排序、“上移”“下移”或拖动完成调整后，请点击“保存全部布局”。

## 6. 添加和编辑窗口

“添加窗口”和“编辑窗口”都会打开“窗口设置”窗口。

### 6.1 字段说明

| 字段 | 填写方法 |
| --- | --- |
| 名称 | 自定义显示名称，仅用于识别和日志显示，例如“财务后台”或“钉钉”。不能为空。 |
| 应用程序 | 目标 EXE 的完整路径。可以“浏览…”，也可以用 Edge 或 Chrome 快速选择。 |
| 启动类型 | 浏览器网址通常勾选“使用 --new-window 创建独立浏览器窗口”；普通桌面应用不要勾选。 |
| 浏览模式 | 从“普通”“访客”“无痕”中选择一项。程序会根据 Edge 或 Chrome 自动使用正确启动参数。 |
| 浏览器网址 | 每行填写一个标签页网址。点击标题下方的“+”添加空白网址行，点击对应行右侧“−”直接删除该行；列表不会使用额外的占位输入行。网址行少时会完整显示，超出窗口可用高度后才启用无箭头滚动条。 |
| 其他参数 | 仅用于少见的高级命令行参数；访客、无痕和网址不需要填在这里。 |
| 工作目录 | 应用启动时使用的目录。多数程序可以留空；依赖相对路径的旧软件可指定。 |
| 目标显示器 | 选择窗口要放置的屏幕。列表会显示屏幕编号、设备名、工作区尺寸及主屏标记。 |
| 位置预设 | 全屏工作区、左右半屏、四个四分之一位置或自定义。 |
| 位置（像素） | `X`、`Y`、`宽`、`高`，均以目标显示器工作区左上角为原点。 |
| 启动前等待 | 启动此项目之前等待的毫秒数，范围 0～60000。 |
| 找窗口超时 | 启动后等待窗口出现的最长时间，范围 1000～120000 毫秒，默认 15000 毫秒。 |

### 6.2 工作区与像素位置

“工作区”通常指扣除 Windows 任务栏后的可用屏幕区域。位置采用相对于所选显示器工作区的坐标：

- `X = 0, Y = 0` 表示工作区左上角；
- `宽`和`高`是窗口的目标尺寸；
- `X + 宽`不能超过工作区宽度；
- `Y + 高`不能超过工作区高度。

Chrome 和 Edge 等程序可能有肉眼看不到的窗口边框。软件会尽量补偿这些边框，使实际可见边缘贴合目标位置。

### 6.3 时间设置建议

- 普通软件可以先使用默认值；
- 程序启动较慢、登录组件较多时，可把“找窗口超时”增加到 30000～60000 ms；
- 连续启动多个重量级软件时，可给后面的项目设置 1000～3000 ms 的“启动前等待”；
- 如果日志频繁显示没有找到窗口，应先增加超时，再检查是否选错 EXE。

## 7. 浏览器窗口设置

### 7.1 普通独立浏览器窗口

1. 在“应用程序”处选择 Edge 或 Chrome。
2. 勾选“使用 --new-window 创建独立浏览器窗口”。
3. 选择“普通”浏览模式。
4. 填写第一个完整网址；同一窗口需要多个标签页时，点击“+”继续填写。
5. 只有需要独立摆放的另一个浏览器窗口，才需要新建另一个窗口项目。

软件会按列表顺序逐个启动，以降低多个浏览器窗口同时出现时的匹配错误。

### 7.2 访客与无痕模式

直接选择“访客”或“无痕”，不用手动填写命令行参数。Edge 无痕会自动使用 `--inprivate`，Chrome 无痕会自动使用 `--incognito`，访客模式会使用 `--guest`。三种模式互斥，每个窗口只能选一种。

### 7.3 同一窗口的多个网址

每行填写一个网址。点击“+”会在最后一行下方增加一行空白网址栏，但不会强制把焦点跳到新行；再次点击网址栏后即可编辑。点击某行右侧“−”会直接删除对应网址行，不会生成额外输入框。启动时，所有网址会在同一个浏览器窗口中按顺序打开为多个标签页。

网址区域会根据窗口当前可用高度显示尽可能多的完整行；窗口变高时会显示更多网址，窗口变矮或网址较多时才出现右侧无箭头滚动条。滚动条支持鼠标拖动、滚轮和键盘方向键/翻页键，添加或删除网址不会留下不可操作的空行。

“其他参数”仅用于少见的高级需求，例如 `--disable-extensions`。不要再把访客、无痕参数或网址填入该栏。

### 7.4 浏览器会话带来的影响

以下情况可能干扰新窗口识别：

- 浏览器恢复上次会话；
- 扩展程序自动弹出窗口；
- 浏览器首次运行提示或更新提示；
- 浏览器已经在后台运行并接管新请求；
- 同一时间由其他程序打开同一个浏览器窗口。

出现问题时，先关闭无关弹窗，再增加“找窗口超时”，并让布局中的浏览器窗口串行启动。

### 7.5 刷新已经打开的窗口和标签页

窗口由软件启动后，如果手动移动或调整了大小，可以在主列表中右键对应项目，选择“刷新窗口”。程序会读取当前显示器、位置和大小；如果是浏览器，还会对比实际标签页与当前配置，并先列出具体变更；确认后才会写入配置。

顶部“一键刷新网址”会检查当前布局中的所有浏览器项目，并先显示变更确认清单：完整读取到标签页的窗口会按实际情况新增或删除网址；没有完整读取的窗口只补充网址，不删除原配置；没有找到对应窗口的项目会跳过。确认后才会保存变更。

刷新期间软件会短暂逐个选择浏览器标签页读取地址栏，并在结束后恢复原来的标签页，同时恢复刷新前的前台窗口。请等刷新完成后再手动切换标签页。隐私窗口、浏览器内部页面、权限较高的窗口或未向 Windows 无障碍接口公开网址的标签页可能无法读取，具体情况会写入状态栏和日志。

## 8. 捕获已经摆好的窗口

如果窗口已经手动摆好，可以直接捕获：

1. 先从“布局方案”下拉框选择目标布局。
2. 把目标窗口摆到需要的位置和大小。
3. 回到管理器，点击“3秒后捕获窗口”。
4. 阅读提示并点击“确定”。
5. 管理器隐藏后，在 3 秒内点击目标窗口，使其位于最前方。
6. 倒计时结束后，管理器会重新出现并打开“窗口设置”。
7. 检查程序路径、名称、显示器、位置和浏览器设置，然后点击“确定”。
8. 点击“保存全部布局”。

捕获读取的是倒计时结束瞬间的前台窗口。如果是 Edge 或 Chrome，会通过 Windows UI Automation 尝试读取全部可读取标签页网址，并识别访客或无痕模式。读取完成后会把网址带入“窗口设置”中；无法读取的内容仍可在编辑窗口中手动补充。如果捕获到错误窗口，删除该项目后重新操作即可。

捕获到的窗口只有在确认保存进布局后才会加入运行时追踪，并在主列表中显示为“已打开”；因为捕获本身就是对当前实际窗口的记录。

### 8.1 一键捕获当前桌面的全部窗口

点击“一键捕获所有窗口”后会立即扫描当前桌面，不进行倒计时，也不会隐藏管理器。扫描完成后会显示确认窗口：

1. 默认勾选所有捕获到的窗口；
2. 可点击“全选”“全不选”，也可逐项勾选需要保存的窗口；
3. 点击“保存为新布局”并输入新名称，可建立一个独立布局；
4. 已在主界面选择布局时，可点击“替换现有布局”，再次确认后覆盖该布局的窗口配置；
5. 替换配置不会关闭当前桌面上已经打开的窗口；
6. 浏览器窗口会自动识别为浏览器新窗口，并通过 Windows UI Automation 尝试读取当前活动标签页的网址；
7. Chrome 隐身窗口、Edge InPrivate 和来宾窗口会根据窗口标题自动选择“无痕”或“访客”浏览模式；
8. 确认列表会分别显示“已读取当前页”“已识别模式”或“网址需手动填写”；
9. “3秒后捕获窗口”会尝试自动填写全部可读取标签页；“一键捕获所有窗口”仍只读取当前活动标签页。保存后可在主列表右键项目并选择“刷新窗口”，重新同步窗口位置、大小及浏览器其他标签页；无法读取的标签页可在编辑窗口中点击“+”补充；
10. 如果浏览器版本、语言或无障碍接口限制导致网址读取失败，窗口仍会正常加入确认列表，网址保持为空，不会猜测错误地址。

如果浏览器正在切换标签页或暂时忙碌，Windows UI Automation 可能返回“服务器忙，请稍后重试”（`0x8001010A`）。程序会自动进行有限次数重试；仍失败时仅跳过网址读取，捕获窗口和位置仍会继续，不会弹出 JIT 调试异常。

程序会自动排除自身、桌面、任务栏、隐藏窗口、最小化窗口、无标题窗口和尺寸过小的辅助窗口。

## 9. 布局管理

### 9.1 新建、复制、重命名和删除

- “新建”适合建立完全不同的窗口组合；
- “复制”适合在现有布局基础上调整少数网址或位置；
- “重命名”只改变名称，不改变窗口配置；
- “删除”会删除布局及其窗口配置，但不会立即关闭桌面上的程序；
- 布局名称不能重复；
- 软件始终要求至少保留一个布局。

### 9.2 布局管理窗口

点击“布局管理”后，可以：

- 查看每个布局的顺序、名称、窗口数、创建时间和最后修改时间；
- 点击“名称”“窗口数”“创建时间”或“最后修改时间”表头排序，再次点击可切换升序或降序；
- 拖动表头分隔线可调整列宽，向右拖动会保持左边界不动并向右扩展；
- 点击左上角“导入布局”，选择布局包或 JSON 配置文件；导入前会显示布局名称和窗口数预览；
- 导入时遇到同名布局会自动改为“名称 (2)”“名称 (3)”等，不会自动启动布局；
- 点击左上角“导出布局”，勾选一个或多个布局后可批量导出；没有勾选时导出当前高亮布局；
- 右键布局并选择“导出此布局”，可直接导出单个布局；导出文件默认使用 `.wlm.json` 格式；
- 首列勾选用于批量删除；“删除”按钮会根据是否有勾选项，自动删除勾选布局或当前布局；
- 在列表中单击布局进行选择；按住 `Shift` 点击可连续高亮多个布局，按住 `Alt` 点击可添加或取消不连续的布局；
- 多选布局后，点击任一已高亮行的首列复选框，会同时勾选或取消勾选全部高亮布局；
- 点击布局管理窗口内列表以外的非按钮区域，可以取消全部高亮选择；
- 右键布局选择“编辑布局”，或双击布局，可关闭管理窗口并回到主界面编辑该布局；
- 复制、重命名或删除当前布局；
- 点击首列表头复选框全选或取消全选；
- 双击某个布局，保存管理结果并在主界面显示并编辑该布局；
- 点击“保存”应用修改，点击“取消”放弃本次布局管理窗口中的修改。

导入或删除操作完成后，仍需点击“保存”才会写入配置文件。导入只增加布局配置，不会关闭当前桌面窗口，也不会自动启动导入的布局。

## 10. 启动、切换与关闭窗口

### 10.1 启动高亮项目

选择一行或多行后展开“启动 ▼”，选择“启动选中项”，只执行这些项目。双击一行仍可直接启动该窗口。

部分启动后，软件会把成功打开的窗口加入本次运行的追踪记录，但不会把未选择的项目一起打开。

### 10.2 全部启动当前布局

“全部启动”按列表顺序执行当前布局中的全部项目，并保存当前配置。

如果当前显示布局和上一次实际启用布局不同，软件会先显示即将关闭的前一布局窗口清单。确认后，软件先尝试关闭前一布局的已追踪窗口，再继续启动新布局。单个旧窗口关闭失败不会阻止新布局继续启动，失败信息会写入日志。

### 10.3 启动与关闭菜单

主界面只保留“启动 ▼”“关闭 ▼”“全部启动”“全部关闭”四个运行按钮。菜单展开时箭头由 `▼` 变为 `▲`，再次点击按钮可收起。“启动”菜单包含启动选中项、全部浏览器窗口及 Edge、Chrome、桌面应用分类启动；“关闭”菜单包含关闭选中项、关闭当前布局，以及浏览器窗口、Edge、Chrome、桌面应用分类关闭。

分类启动只处理当前显示布局，保持原列表顺序，不启动被禁用的项目。“一键启动浏览器窗口”会启动当前布局中所有浏览器项目，包括 Edge、Chrome 以及其他浏览器；“一键关闭浏览器窗口”也会关闭这三类浏览器项目。Edge 和 Chrome 分类仍可单独启动或关闭。分类关闭会先显示即将关闭的窗口或应用清单，确认后才执行。对应类别为空时菜单项自动变灰。

### 10.4 关闭功能的区别

| 操作 | 关闭范围 | 适用场景 |
| --- | --- | --- |
| 关闭 | 当前蓝色高亮项目对应的窗口或应用。 | 临时关闭布局中的少数项目。 |
| 关闭当前布局 | 当前显示布局中逐项匹配到的窗口；同一程序的其他窗口不会按程序批量关闭。 | 只清理当前正在查看的布局。 |
| 全部关闭 | 所有布局中配置过的浏览器和桌面应用。 | 下班、演示结束或需要清空全部配置相关程序。 |

关闭操作执行前会显示确认清单，请认真核对。

### 10.5 重要关闭风险

- 布局切换时关闭旧布局使用已验证的窗口追踪记录，范围相对精确；
- “关闭”和“关闭当前布局”需要根据配置逐项查找目标窗口；“全部关闭”按程序查找目标；
- 对 Chrome、Edge 等浏览器执行按程序关闭时，可能同时关闭该浏览器中手动打开、但没有写入布局的其他可见窗口；
- 桌面应用会先尝试正常关闭；仍在后台或托盘运行时，软件可能结束相应进程；
- 未保存的文档、表格、聊天草稿或网页表单可能丢失；
- 执行前应先保存其他软件中的工作，并阅读确认窗口列出的内容。

## 11. 保存、配置与自动备份

### 11.1 配置保存位置

所有 Windows 布局都保存在当前 Windows 用户的本地目录：

```text
%LOCALAPPDATA%\WindowLayoutManager\config.json
```

可以点击“打开配置目录”直接进入该位置。

配置不放在 EXE 旁边，因此：

- 更新或替换 EXE 通常不会删除已有布局；
- 把程序文件夹发给其他用户，不会自动把你的布局发给对方；
- 其他用户启动新版 EXE 后，会读取其自己的 Windows 用户目录中以前保存的布局；
- 同一台电脑的不同 Windows 用户拥有各自独立的布局。

### 11.2 何时保存

建议在以下操作后点击“保存全部布局”：

- 添加、编辑、复制、删除或重新排序窗口；
- 新建、复制、重命名或删除布局；
- 调整显示器或窗口像素位置。

启动、切换布局、缩到托盘或完全退出时，软件也会尝试保存配置，但完成重要修改后主动保存更稳妥。

### 11.3 配置备份文件

软件会在配置目录维护历史或迁移备份，例如：

```text
config.multilayout.bak
config.json.bak
config.before-multilayout.json
config.before-browser-v3.json
```

这些文件用于配置恢复，不是重复运行文件。除非确认不再需要恢复旧配置，否则不要随意删除或编辑。

### 11.4 运行状态文件

软件把窗口追踪身份保存在：

```text
%LOCALAPPDATA%\WindowLayoutManager\runtime-state.json
```

它只记录窗口句柄、进程身份和布局项目 ID 等运行状态，不包含一套新的布局配置。Windows 重启、窗口关闭或身份校验失败后，无效记录会被自动忽略或清理。

## 12. 托盘运行与完全退出

点击主窗口右上角的关闭按钮时，软件不会完全退出，而会：

1. 隐藏主界面；
2. 保存配置；
3. 继续在系统托盘运行；
4. 保留本次窗口追踪信息。

恢复主界面的方法：

- 双击系统托盘中的窗口布局管理器图标；
- 右键托盘图标，选择“显示主界面”；
- 再次双击 `窗口布局管理器.exe`。

完全退出的方法：

1. 右键系统托盘图标；
2. 选择“退出程序”；
3. 在确认窗口中选择“是”。

完全退出管理器不会自动关闭已经打开的桌面窗口。

## 13. 换电脑、更新版本和分发

### 13.1 在同一台电脑更新程序

1. 从托盘中完全退出旧版；
2. 下载或取得新版 `WindowLayoutManager-Setup-v1.1.0.exe` 安装包；
3. 双击运行安装器，按向导完成覆盖安装；
4. 从开始菜单或桌面快捷方式启动新版软件。

安装器只会更新程序文件，不会删除 `%LOCALAPPDATA%\WindowLayoutManager` 中原先保存的布局。

从 v1.0.2 起，也可以在软件的“关于”窗口中检查更新。软件启动后会在后台定期检查公开发布仓库；发现新版时，打开“关于”并点击“检查更新”，确认完整的版本说明后点击“下载更新”。下载完成并校验通过后，软件会自动启动安装器；如果取消下载或安装，会继续使用当前版本。检查更新不会上传布局数据；没有网络或发布仓库暂时不可用时，不影响软件正常使用。

### 13.2 发给其他用户

只需发送一个同版本的安装包：

```text
WindowLayoutManager-Setup-v1.1.0.exe
```

首次使用者双击安装包即可安装。已经安装过软件的用户，应先从托盘完全退出旧版，再运行新的安装包进行覆盖更新；其原有布局会保留在自己的 Windows 用户目录中。

### 13.3 把自己的布局迁移到一台全新电脑

推荐使用“布局管理”中的导出和导入功能迁移布局：

1. 在旧电脑打开“布局管理”，勾选需要迁移的一个或多个布局，点击“导出布局”；
2. 将生成的 `.wlm.json` 文件复制到新电脑；
3. 在新电脑打开“布局管理”，点击“导入布局”，确认预览内容后点击“保存”。

导入会自动生成新的布局和窗口项目 ID，遇到同名布局会自动重命名。它不会迁移正在运行的窗口追踪状态，也不会自动启动布局。

如果需要完整迁移所有配置，也可以在旧电脑退出软件后，单独复制：

```text
%LOCALAPPDATA%\WindowLayoutManager\config.json
```

然后放到新电脑相同的当前用户目录。配置可能包含网址、程序路径和个人工作信息，只应在可信设备之间传输。

换电脑后还要检查：

- 应用程序安装路径是否相同；
- 显示器数量、编号、设备名和排列顺序是否变化；
- 分辨率、缩放比例和任务栏位置是否变化；
- 自定义像素位置是否仍在新屏幕工作区范围内。

不要把旧电脑的 `runtime-state.json` 当作布局迁移；其中的窗口和进程身份只对原电脑会话有效。

## 14. 常见问题与排查

### 14.1 启动后列表是空的

先从“布局方案”下拉框手动选择布局。软件设计为启动时不自动选择配置，也不会自动打开任何窗口。

### 14.2 点击“启动”或“关闭”没有反应

请确认至少有一行处于蓝色高亮状态。“启动”和“关闭”只作用于高亮项目；要运行整个布局应使用“全部启动”。

### 14.3 程序启动了，但窗口没有移动

依次检查：

1. 底部日志是否显示“找窗口超时”；
2. 配置的 EXE 是否确实是产生该窗口的程序；
3. 是否需要增加“找窗口超时”；
4. 目标程序是否以管理员身份运行；
5. 如目标程序是管理员权限，尝试右键本软件并选择“以管理员身份运行”；
6. 目标程序是否把界面放在另一个进程中；
7. 是否出现登录框、更新框或扩展弹窗干扰识别。

### 14.4 窗口去了错误的显示器

打开该项目的“编辑窗口”，重新选择目标显示器并保存。如果更换过显示器、显卡接口或远程桌面环境，Windows 的显示器设备名和顺序可能发生变化。

### 14.5 浏览器打开了错误的页面或错误窗口

- 确认同一窗口的多个网址已分行填写；需要独立摆放的窗口才单独建立项目；
- 确认勾选浏览器新窗口；
- 关闭浏览器恢复会话和无关扩展弹窗后重试；
- 增加超时时间；
- 避免布局执行时手动同时打开同一个浏览器的新窗口。

### 14.6 点击右上角后程序没有退出

这是正常行为。右上角关闭按钮表示缩到托盘。需要完全退出时，请使用托盘菜单中的“退出程序”。

### 14.7 找不到托盘图标

点击任务栏通知区域的向上箭头，查看隐藏图标。也可以再次双击 EXE，已运行的实例会重新显示主界面。

### 14.8 配置读取失败

1. 点击“打开配置目录”，或按 `Win + R` 输入 `%LOCALAPPDATA%\WindowLayoutManager`；
2. 完全退出软件；
3. 先复制整个目录作为临时备份；
4. 检查 `config.json` 是否为空、被截断或被手动改坏；
5. 必要时从 `.bak` 备份恢复；
6. 不确定时不要直接覆盖现有文件，应先保留副本。

## 15. 当前版本限制

- 暂不处理 Windows 虚拟桌面；
- 暂不处理浏览器 F11 全屏；
- 以更高权限运行的应用可能无法由普通权限的管理器移动；
- 某些采用多进程、启动器或特殊渲染框架的应用可能无法准确识别窗口；
- 浏览器会话恢复、扩展弹窗和后台进程可能影响新窗口匹配；
- 更换显示器、分辨率或任务栏位置后，自定义像素布局需要重新检查；
- 关闭浏览器类项目时，可能影响同一浏览器中未写入布局的其他窗口。

## 16. 技术附录

### 16.1 数据与程序的关系

```text
程序文件夹
├─ 窗口布局管理器.exe          程序本体
└─ 窗口布局管理器.exe.config   .NET 运行配置

%LOCALAPPDATA%\WindowLayoutManager
├─ config.json                  全部布局配置
├─ runtime-state.json           当前窗口追踪状态
└─ config*.bak / *.json         自动备份或历史迁移备份
```

替换程序文件不会主动覆盖用户配置；删除程序文件也不会自动删除 `%LOCALAPPDATA%` 中的布局。

### 16.2 开发包目录

| 目录或文件 | 用途 |
| --- | --- |
| `src` | C# WinForms 源代码。 |
| `tests` | 自动化冒烟测试源码。 |
| `assets` | 应用图标等资源。 |
| `tools` | 开发辅助工具。 |
| `发布` | 编译后的程序文件和生成的安装包。 |
| `build.ps1` | 使用 Windows 自带 .NET Framework C# 编译器构建。 |
| `build-installer.ps1` | 构建程序、编译安装器，并把用户手册复制到 `发布` 目录。 |
| `test.ps1` | 编译并运行自动化测试。 |
| `README.md` | 项目功能与开发说明。 |
| `用户手册.md` | 面向最终用户的本手册。 |

### 16.3 构建与测试

在开发包目录打开 PowerShell：

```powershell
.\test.ps1
.\build.ps1
.\build-installer.ps1
```

运行 `build.ps1` 会更新：

```text
发布\WindowLayoutManager-v1.1.0.exe
发布\WindowLayoutManager-v1.1.0.exe.config
```

运行 `build-installer.ps1` 还会生成 `发布\WindowLayoutManager-Setup-v1.1.0.exe`，并同步输出 `发布\WindowLayoutManager-UserGuide.md`；安装器会把同一份用户手册安装到程序目录。

---

如果遇到异常，优先点击顶部工具栏最右侧的“导出日志”，并同时记录具体操作步骤。诊断文件包含软件版本、Windows 版本、目标程序名称和路径、显示器数量与分辨率、布局配置、窗口追踪记录以及底部日志中的完整错误内容；其中可能包含网址、启动参数等敏感信息，发送前请先检查。
